# HTML Resume

In this assignment we have to make the html resume just we need to convet the design into code.

### deploy link: [preview]()

### If you want to clone the repo
```shell
git clone link
```